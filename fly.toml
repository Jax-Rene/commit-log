app = "commitlog"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"
  DATABASE_PATH = "/data/commitlog.db"
  GIN_MODE = "release"

[[mounts]]
  source = "commitlog_data"
  destination = "/data"
  initial_size = "1GB"

[http_service]
  internal_port = 8080
  force_https = true
  min_machines_running = 1

[checks]
  [checks.healthz]
    type = "http"
    port = 8080
    interval = "10s"
    timeout = "2s"
    grace_period = "20s"
    method = "GET"
    path = "/healthz"

